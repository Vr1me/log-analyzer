# Log Analyzer (Python)

A simple Python tool that analyzes a log file and counts ERROR and WARNING entries.

## Features
- Counts the number of `ERROR` and `WARNING` lines
- Handles missing file errors gracefully

## How to run
```bash
python main.py

Git email fixed